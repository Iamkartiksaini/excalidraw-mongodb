## What to build

Create the `getSharedWithMeNotes` server action (fetching items where user's email is in `invitedEmails`). Build the UI tab in the dashboard to display these items.

## Acceptance criteria

- [x] `getSharedWithMeNotes` server action accurately retrieves notes and folders based on the current user's email
- [x] Dashboard sidebar/tabs updated to include a "Shared with me" section
- [x] UI successfully renders the list of shared items

## Blocked by

- 03-unified-share-dialog.md

## Status
Completed

## Progress Summary
| Date | Status | Notes |
|---|---|---|
| 2026-05-17 | Completed | getSharedWithMeNotes server action, "Shared with me" tab integration in DashboardClient, custom queries, dynamic counts, and empty states implemented successfully. |
